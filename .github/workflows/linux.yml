name: linux
run-name: Linux Workflow
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
      - name: Build ISO Image
        run: make container CMD="make all"
